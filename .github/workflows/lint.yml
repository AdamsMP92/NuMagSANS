name: Linting

on: [push, pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install compiler
        run: sudo apt-get update && sudo apt-get install -y clang

      - name: Check specific files
        run: |
          echo "🔍 Running syntax check on selected headers and CUDA sources..."
          clang -x cuda -std=c++11 -D__CUDACC__ -fsyntax-only \
            -I./src -I/usr/local/cuda/include \
            src/NuMagSANSlib_Directory.h \
            src/NuMagSANSlib_HelperFun.h
            src/NuMagSANSlib_InputFileInterpreter.h
            src/NuMagSANSlib_LogFile.h
          echo "✅ Manual syntax check completed successfully."
